//BMIの変数を宣言
let BMI;

//BMIの変数を代入
BMI = ('体格指数');

//体重を指定
weight = (68);

//身長の変数を宣言
let height;

//身長を指定
height = (1.7);

// BMIを求める計算を実行
console.log(BMI + 'は' + (weight / (height * height)) + 'です。'); 